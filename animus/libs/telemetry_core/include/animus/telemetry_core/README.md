# telemetry_core Public Headers

`animus::telemetry_core` is intentionally delayed until terrain is stable. It
will later own telemetry samples, tracks, entities, timelines, events, and log
ingestion contracts.
